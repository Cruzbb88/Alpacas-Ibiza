<!--
  SCORM Unit Registration Template (v1.0 — Feb 2026)
  Generated by: /scorm scaffold

  This template shows what to add to register a new SCORM unit.
  Three files need updating: build-scorm.ts, App.tsx, and types.ts.

  IMPORTANT: Escape & as &amp; in titles (e.g., "Q&A" -> "Q&amp;A")
-->

<!-- ============================================================ -->
<!-- 1. Add to UNITS array in build-scorm.ts                       -->
<!-- ============================================================ -->
<!--
  // In scripts/build-scorm.ts, add to the UNITS array:
  {
    unitType: '{{UNIT_TYPE}}',
    zipName: '{{ZIP_NUMBER}}-{{UNIT_SLUG}}.zip',
    title: '{{UNIT_TITLE}}',
    identifier: 'esei-{{UNIT_SLUG}}',
    // Optional: courseId: 'course_123',
  },
-->

<!-- ============================================================ -->
<!-- 2. Add lazy import + route in App.tsx                          -->
<!-- ============================================================ -->
<!--
  // At the top of src/App.tsx, add the lazy import:
  const {{PascalName}} = lazy(() => import('./units/{{kebab-name}}/{{PascalName}}'));

  // In the unitComponents record, add:
  {{unit_type}}: {{PascalName}},
-->

<!-- ============================================================ -->
<!-- 3. Add to UnitType union in src/lib/types.ts                  -->
<!-- ============================================================ -->
<!--
  // In the UnitType union:
  | '{{unit_type}}'
-->

<!-- ============================================================ -->
<!-- 4. Create unit directory structure                             -->
<!-- ============================================================ -->
<!--
  src/units/{{kebab-name}}/
    ├── {{PascalName}}.tsx    (main component, from template)
    ├── index.ts              (export default from './{{PascalName}}')
    └── types.ts              (optional, unit-specific types)

  index.ts content:
    export { default } from './{{PascalName}}';
-->

<!-- ============================================================ -->
<!-- 5. Generated imsmanifest.xml (for reference)                  -->
<!-- ============================================================ -->
<!--
  The build-scorm.ts generateManifest() function creates this
  automatically. You do NOT need to manually create it.

  Generated manifest for unit_type={{UNIT_TYPE}}:
-->
<?xml version="1.0" encoding="UTF-8"?>
<manifest identifier="esei-{{UNIT_SLUG}}"
  version="1.0"
  xmlns="http://www.imsproject.org/xsd/imscp_rootv1p1p2"
  xmlns:adlcp="http://www.adlnet.org/xsd/adlcp_rootv1p2"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.imsproject.org/xsd/imscp_rootv1p1p2 imscp_rootv1p1p2.xsd
                       http://www.imsglobal.org/xsd/imsmd_rootv1p2p1 imsmd_rootv1p2p1.xsd
                       http://www.adlnet.org/xsd/adlcp_rootv1p2 adlcp_rootv1p2.xsd">
  <metadata>
    <schema>ADL SCORM</schema>
    <schemaversion>1.2</schemaversion>
  </metadata>
  <organizations default="esei-org">
    <organization identifier="esei-org">
      <title>{{UNIT_TITLE}}</title>
      <item identifier="esei-item-1" identifierref="esei-resource-1">
        <title>{{UNIT_TITLE}}</title>
      </item>
    </organization>
  </organizations>
  <resources>
    <resource identifier="esei-resource-1" type="webcontent" adlcp:scormtype="sco"
              href="index.html?unit_type={{UNIT_TYPE}}">
      <file href="index.html" />
    </resource>
  </resources>
</manifest>

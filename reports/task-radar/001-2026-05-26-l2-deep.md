# Task Radar -- Alpacas Ibiza site (L2 Deep, 2026-05-26)

## Methodology + degraded-mode note

L2 Deep scan of `alpaca-farm-redesign` using local-file-only sources. Catalog 005 (hook-enforced) blocks all `mcp__omni-cortex__*` calls; sticky notes substituted by grepping TODO/FIXME/UNMAPPED/STOP across `*.ts`/`*.tsx`, reading OWNER_INPUT_NEEDED.md, PLAN.md, CANT_BE_DONE.md, specs/todo/, philosophy-prompting catalog recurrence logs, and the prior L1 report (tr-001). No Cortex recall, no Brain status, no prompt mining.

## Inventory

- **Grep hits:** 63 across 16 files (TODO: 5 actionable, UNMAPPED: 42 sentinels across 8 files, test scaffolding: 13 in validate-env.test.ts, skill templates: 3 in .claude/skills/scorm/)
- **OWNER_INPUT_NEEDED items:** 9 warning, 8 yellow, 12 green
- **PLAN.md unresolved:** Track A fully shipped (A1-A6). Track B = 3 specs in todo (all owner-blocked). Track C (C1 language strategy, C2 alpaca bios, C3 Wishfulfilling Weaving) -- all merged into OWNER_INPUT_NEEDED.md already.
- **specs/todo/:** 3 specs (002 GDPR legal, 003 Adopt-a-Paca, 005 locale strategy) -- all owner-blocked
- **specs/done/:** 5 specs (001, 004, 006, 007, 008)
- **CANT_BE_DONE.md:** 10 hard limits (Cortex, GA4 live, FareHarbor API, admin creds on Vercel, hex values, image assets, git history, DOM admin, Lighthouse, decision decay)
- **Catalog FAIL recurrences:** 2 (007 verify-doc-cross-quotes, 008 re-read-after-cross-tool-modifications) -- both logged marginal FAILs on 2026-05-26

## Verification of prior report (tr-001) items

The prior L1 report listed 8 Q1 items. Verifying current state:

| tr-001 Q1 Item | Status | Evidence |
|---|---|---|
| Privacy/Terms/Cookies placeholder | ACTIVE | `app/[locale]/privacy/page.tsx:16` checks `isUnmapped`; translation keys still `[UNMAPPED` |
| ADMIN default creds | RESOLVED | Code fail-closed: `route.ts:19-20` returns null + console.error when unset; no default fallback |
| Tour price split 20/30 | RESOLVED | `lib/config.ts:17` exports `TOUR_BASE_PRICE_EUR = 30`; `structured-data.ts:120` imports it. Spec 001 done. |
| Hardcoded reviewCount 127 | RESOLVED | `structured-data.ts:91` now conditional on `reviewData?.ratingValue != null`. Spec 006 done. |
| Dead non-localized routes | RESOLVED | `app/shop/page.tsx`, `app/about/page.tsx`, `app/contact/page.tsx` deleted. Spec 004 done. |
| Alpaca bios null | ACTIVE | `lib/data/alpacas.ts:5` -- all 14 bios + images still `null` (UNMAPPED) |
| RESEND_API_KEY Tier 1 | ACTIVE | Deploy-time concern. `lib/validate-env.ts:26` checks it; code throws without it. Owner must set. |
| Adopt-a-Paca page missing | RESOLVED | `app/[locale]/adopt/page.tsx` exists with verified EUR 75/mo pricing + 9 benefits |

**Net: 4 of 8 Q1 items resolved since tr-001.**

## Eisenhower matrix

### Do now -- Urgent + Important

1. **`app/[locale]/privacy/page.tsx:16`** -- Privacy/Terms/Cookies pages render `[UNMAPPED` placeholder text. GDPR legal risk for EU-registered business. Owner must supply real text. Spec 002 tracks this.
2. **`lib/data/alpacas.ts:5,12`** -- All 14 alpaca bios + images `null`. Adopt page and /alpacas route render empty cards. Owner must supply bios + photos. Spec 003 tracks this.
3. **`app/[locale]/adopt/page.tsx:224-226`** -- Adopt page built but 3 UNMAPPED items: payment vendor (FareHarbor/Stripe/Mollie), per-alpaca cap, nav placement. Owner decision required before launch.
4. **`lib/payment-vendor.ts:66,124`** -- Two TODO stubs: Stripe checkout session API route and Mollie subscription API route. Dead code until owner picks a payment vendor for adopt subscriptions.

### Important not urgent -- Schedule

5. **`specs/todo/005-locale-strategy.md`** -- 6 locales configured, `en` default, GB flag. Owner must decide: keep IT/FR? Change default to `nl`? Fix flag emoji?
6. **OWNER_INPUT_NEEDED.md:32-35** -- GTM container: code has only `GTM-KR3CGLS6` (FareHarbor); docs reference `GTM-NJRGZPGS`. CLAUDE.md now notes NJRGZPGS doesn't exist in code. Owner must clarify. (RESOLVED in docs -- CLAUDE.md explicitly says moot until owner supplies a container ID.)
7. **OWNER_INPUT_NEEDED.md:38-41** -- Cancellation policy "24h" unverified against FareHarbor setting.
8. **OWNER_INPUT_NEEDED.md:44-51** -- Per-tour pricing not confirmed (conversion anchor missing from cards).
9. **`app/[locale]/shop/woven/page.tsx:19`** -- Woven product prices `null` (UNMAPPED). Owner must confirm.
10. **`app/[locale]/shop/alcaca/page.tsx:19`** -- Alcaca product prices `null` (UNMAPPED). Owner must confirm.
11. **`app/[locale]/sustainability/page.tsx:39,52,113-116`** -- 4 UNMAPPED facts (finca size, natural shades count, certifications, hero image). Owner must verify.
12. **`app/[locale]/yoga/page.tsx:16`** -- 6 UNMAPPED fields (instructor, start time, season, what-to-bring, cancellation, photos + FareHarbor item ID).
13. **`lib/data/press.ts:24-60`** -- 6 press entries with `articleUrl: null, logoUrl: null` (12 UNMAPPED sentinels). Owner must supply URLs + logos.
14. **OWNER_INPUT_NEEDED.md:78-83** -- Spanish legal footer (CIF, registered name, address) missing.
15. **OWNER_INPUT_NEEDED.md:145-149** -- Vercel deploy + custom domain + strong admin creds not set.

### Urgent not important -- Delegate / one-shot

16. **Catalog 007 FAIL recurrence** -- `007-verify-doc-cross-quotes.md:74`: parent Opus session trusted a migration agent's count without re-reading catalog/. Mechanism was stale-trust even though the fact was correct. Low-impact, advisory only.
17. **Catalog 008 FAIL recurrence** -- `008-re-read-after-cross-tool-modifications.md:71`: `lib/config.ts` duplicate `TOUR_BASE_PRICE_EUR` from cross-tool blindness. Already fixed (spec 001 done), but the catalog entry stays pending -- enforcement hook not yet built.

### Drop

18. **`app/[locale]/terms/page.tsx:12`** -- UNMAPPED sentinel handling code. Not a task -- this is the correct defensive pattern. No action.
19. **`.claude/skills/scorm/templates/*.tsx` TODOs** -- 3 TODO markers in skill templates. Not project code; not shipped. No action.

## Top 3 next moves

1. **Owner call: GDPR + alpaca bios + locale decision.** Starting point: OWNER_INPUT_NEEDED.md "Three conversations" section (line 325). Unlocks specs 002, 003, 005 simultaneously. Single 30-minute call resolves all 3 do-now blockers.
2. **Payment vendor decision for Adopt.** Starting point: `app/[locale]/adopt/page.tsx:224`. Once owner picks FareHarbor vs Stripe vs Mollie, wire the corresponding stub in `lib/payment-vendor.ts:66` or `:124`.
3. **Sustainability / yoga page data fill.** Starting point: `app/[locale]/sustainability/page.tsx:113`. Low-effort owner verification (finca size, shades count) unblocks two pages from UNMAPPED state.

## STOP

Items that cannot be classified without owner:

- **Payment vendor for adopt subscriptions** -- FareHarbor, Stripe, or Mollie? Determines which `lib/payment-vendor.ts` stub gets wired. No technical work until decided.
- **GTM container strategy** -- single (FH-only, current code) or dual? CLAUDE.md says moot until owner explicitly supplies a container ID. Parked.
- **Cancellation policy duration** -- 24h? 48h? Different per tour type? Must match FareHarbor flow. Cannot be verified without FareHarbor admin access (CANT_BE_DONE.md limit).

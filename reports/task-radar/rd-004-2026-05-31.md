---
report_id: rd-004
date: "2026-05-31"
generated_at: "2026-05-31 6:00 PM MDT"
layer: "L2+L3"
previous_report: null
project: alpaca-farm-redesign
items_total: 22
q1_count: 7
q2_count: 8
q3_count: 4
q4_count: 3
items_resolved: 0
completed_this_session: 0
predicted_actions_count: 10
quick_wins_count: 5
quick_wins_total_time: "45m"
maintenance_overdue: []
---

# Task Radar — rd-004 (2026-05-31)

_L2 Deep Radar + L3 Pipeline Audit. First report for this project (no previous radar history — no resolution registry, no item ledger). All items are NEW._

---

## Carry-Forward Audit

No previous report exists. All items sourced fresh from code scan.

---

## Eisenhower Matrix

> Importance scores: spec-sourced = 60, blocking-legal = 90, revenue-blocking = 85, owner-data gap = 55, code-gap = 65, tech-debt = 30–40.
> Urgency scores: base from staleness (FORWARD_PLAN.md written 2026-05-30 = 1d = base 20) + modifiers.
> Age: all items are [0d] (first radar appearance).

---

### Q1 — Do Now (importance ≥ 50, urgency ≥ 50)

| # | Item | Type | Source | I | U | Age | Owner |
|---|------|------|--------|---|---|-----|-------|
| 1 | `adopt-deferred-gifts` cron is a stub — dispatched=0, never sends welcome emails | code-gap | `app/api/adopt-deferred-gifts/route.ts:43` | 80 | 90 | [0d] | — |
| 2 | `success_url` does NOT thread `donor_name`/`alpaca_name` — thank-you page personalisation and certificate download are broken for Stripe checkout donors | code-gap | `components/adopt-thank-you.tsx:142-145`, `app/api/checkout/route.ts:201` | 75 | 85 | [0d] | — |
| 3 | CIF field still `null` in `lib/tenants/alpacasibiza.ts` line 31 — 1-line dev fix, data extracted from live site, owner review note present | integration-pending | `lib/tenants/alpacasibiza.ts:31`, `FORWARD_PLAN.md §2026-05-31` | 85 | 70 | [0d] | — |
| 4 | Full address `streetAddress` is 'San Carlos' — needs 'C/3 Bungalow Park 22'; postal code conflict (07819 vs 07850 from terms page — owner must confirm) | integration-pending | `lib/tenants/alpacasibiza.ts:64-65`, `FORWARD_PLAN.md §2026-05-31` | 85 | 70 | [0d] | — |
| 5 | GDPR consent checkbox absent from newsletter, gift, and adopt flows — GDPR Art. 7 violation, blocks legal launch | legal-blocking | `FORWARD_PLAN.md §1` | 90 | 90 | [0d] | Owner |
| 6 | Legal page body text (privacy, terms, cookies, impressum) still placeholder — `LEGAL_CONTENT_LIVE=false`; Spain LSSI-CE Art. 10 | legal-blocking | `FORWARD_PLAN.md §1` | 90 | 90 | [0d] | Owner |
| 7 | Overlord cycle 5 (E2E Playwright: adopt + booking) not started — `tests/e2e/` dir does not exist; `OVERLORD_QUEUE.md` item 1 primed but unexecuted | code-gap | `OVERLORD_QUEUE.md §Item 1`, `e2e/` (only smoke specs exist) | 65 | 70 | [0d] | — |

---

### Q2 — Schedule (importance ≥ 50, urgency < 50)

| # | Item | Type | Source | I | U | Age | Owner |
|---|------|------|--------|---|---|-----|-------|
| 1 | `recent-bookings-ticker` static pool is empty — owner-blocked (`OWNER_INPUT_NEEDED`); FareHarbor live path not wired (`TODO` in component) | owner-input | `lib/data/social-proof.ts:55-63`, `components/tours/recent-bookings-ticker.tsx:7` | 55 | 30 | [0d] | Owner |
| 2 | Bundle size analysis (Overlord item 2) — routes not yet scanned; `next.config.*` has no `ANALYZE=true` path | code-gap | `OVERLORD_QUEUE.md §Item 2` | 60 | 30 | [0d] | — |
| 3 | Sitemap rebuild after cycle-3 noindex delistings — Overlord item 4 primed but not executed | code-gap | `OVERLORD_QUEUE.md §Item 4` | 60 | 30 | [0d] | — |
| 4 | `i18n` parity — EN keys with no DE/IT/ES/NL/FR counterpart; listed as "not yet attacked" in `OVERLORD_QUEUE.md` | tech-debt | `OVERLORD_QUEUE.md §6` | 55 | 20 | [0d] | — |
| 5 | Quarterly update email herd-news block is a placeholder — `buildQuarterlyUpdateEmail` renders `[Owner action: supply seasonal update...]` on each send | owner-input | `lib/email-templates-retention.ts:82-88`, `FORWARD_PLAN.md §4` | 55 | 20 | [0d] | Owner |
| 6 | Sustainability page: finca size UNMAPPED (visible to real visitors as `[UNMAPPED: confirm finca size in hectares]`) | owner-input | `app/[locale]/sustainability/page.tsx:90-91` | 55 | 20 | [0d] | Owner |
| 7 | Workshop page: price/dates UNMAPPED; renders placeholder to real visitors | owner-input | `JOURNEY_MAP.md §Consideration`, `FORWARD_PLAN.md §3` | 55 | 20 | [0d] | Owner |
| 8 | Alpaca detail page: bio text `localizedBio` populated but `bio` field still `null`; `OWNER_REVIEW_TRANSLATION` flag set on EN translations | owner-input | `FORWARD_PLAN.md §2026-05-31`, `lib/tenants/alpacasibiza-content.ts` | 55 | 20 | [0d] | Owner |

---

### Q3 — Delegate / Automate (importance < 50, urgency ≥ 50)

| # | Item | Type | Source | I | U | Age | Owner |
|---|------|------|--------|---|---|-----|-------|
| 1 | Unsubscribe audit log not wired to persistent store — currently log-only (Vercel Function Logs; GDPR erasure trail gap) | tech-debt | `app/api/newsletter/unsubscribe/route.ts:16` | 35 | 50 | [0d] | — |
| 2 | `format-price.ts` and `retry.ts` are test-only utilities not wired into any UI (`// TODO: used only in tests; wire into UI or remove if obsolete`) | tech-debt | `lib/format-price.ts:1`, `lib/retry.ts:1` | 40 | 50 | [0d] | — |
| 3 | `OWNER_INPUT_NEEDED` on automatic-tax in Stripe checkout (`// OWNER_INPUT_NEEDED: set true once Stripe Tax activated`) | owner-input | `app/api/checkout/route.ts:247` | 40 | 50 | [0d] | Owner |
| 4 | Subscription renewal (`invoice.paid`) — `// TODO: update subscription status in DB on renewal` — DB layer is wired (Drizzle/Neon) but renewal path doesn't call `upsertSubscriptionFromStripe` | code-gap | `app/api/stripe-webhook/route.ts:195`, `lib/db/upsert-from-webhook.ts` | 45 | 50 | [0d] | — |

---

### Q4 — Review / Prune (importance < 50, urgency < 50)

| # | Item | Type | Source | I | U | Age | Owner |
|---|------|------|--------|---|---|-----|-------|
| 1 | `BRAND_THEME_COLOR_HEX` diverges from `BRAND_PRIMARY_HEX` — `#6da855` vs `#556B2F`; may be intentional mobile-chrome accent or copy-paste error | owner-input | `lib/brand.ts:36-39` | 30 | 20 | [0d] | Owner |
| 2 | Instagram handle conflict — structured-data uses `@alpacasibiza`, `lib/tenants/alpacasibiza.ts` uses `@wishfulfillingweaving` — requires owner canonical decision | owner-input | `lib/tenants/alpacasibiza.ts:118-128` | 30 | 20 | [0d] | Owner |
| 3 | `noReplyEmail` field is `null` in tenant config — UNMAPPED; callers fall back to `contactEmail`; noted for when Resend domain is verified | owner-input | `lib/tenants/alpacasibiza.ts:44-48` | 25 | 20 | [0d] | Owner |

---

## Ownership Summary

| Owner | Q1 | Q2 | Q3 | Q4 | Total |
|-------|----|----|----|----|-------|
| —     | 5  | 4  | 3  | 0  | 12    |
| Owner | 2  | 4  | 1  | 3  | 10    |

---

## Specific Gap Investigations (per brief)

### recent-bookings-ticker
**Status: OWNER-BLOCKED (intentional empty start) + TODO code-gap for live path.**
- `lib/data/social-proof.ts:55` — `recentBookings: RecentBookingEntry[] = []` — empty by design (PRACTICES Rule 5: never invent data). Comment says owner must add real entries.
- `components/tours/recent-bookings-ticker.tsx:7` — `TODO: wire when FAREHARBOR_APP_KEY is set` — FareHarbor live path not implemented; component falls through to static pool only.
- Ticker renders `null` when pool empty — page is fine, no visible breakage. **Not urgent until FareHarbor API keys available.**

### adopters-wall / lib/adopters/latest.ts
**Status: FULLY WIRED. `/api/adopt-count` does NOT return latest array — it returns `{ count, source }`. The latest array is a separate module.**
- `lib/adopters/latest.ts` is a real, complete file (216 lines) with Stripe + Mollie paths, 1h in-memory cache, privacy-safe first-name extraction.
- `getLatestAdopters()` is imported directly by `components/adopt/adopters-wall.tsx` (server component) — no HTTP roundtrip.
- `/api/adopt-count` returns only `{ count, source }` — separate concern, different helper (`lib/adopters/count.ts`).
- **No code gap here.** Both modules exist and are wired correctly.

### cancel-feedback route
**Status: FULLY WIRED — not orphaned.**
- `app/api/cancel-feedback/route.ts` — complete POST handler with rate-limit, validation, logging.
- `components/adopt/cancel-feedback-form.tsx:52` — POSTs to `/api/cancel-feedback`.
- `app/[locale]/cancel-feedback/page.tsx:16` — reached from Mollie cancel success redirect (`/api/mollie-manage/cancel` → 303 to `/cancel-feedback?vendor=mollie&sub=...`).
- Stripe path: billing-portal `return_url` configurable in Stripe Dashboard (not code-controlled).
- **Route is wired. No code gap.**

### dunning recovery email (handleStripeInvoicePaid)
**Status: FULLY REACHED — handler IS in the switch.**
- `app/api/stripe-webhook/route.ts:172-197` — `case 'invoice.paid'` block calls `handleStripeInvoicePaid()`.
- `lib/payment-handlers.ts:694` — function exists, resets failure counter, sends recovery email when prior failure existed.
- Switch is complete. The only open item is `// TODO: update subscription status in DB on renewal` (Q3#4 above) — DB upsert not called on renewal path.
- **Handler is reached. DB update is the gap, not the handler wiring.**

### social-proof.ts (empty array)
**Status: INTENTIONAL — not a bug.**
- PRACTICES Rule 5 explicitly prohibits inventing data. Comment documents exactly how to populate.
- Component renders `null` when empty — no broken UI.
- **Not a code gap. Owner-input only.**

### Discord notify (env var only)
**Status: CORRECT — env-var only is the right architecture.**
- `lib/owner-notify.ts` — `sendDiscord()` function exists and is fully wired in `notifyOwner()`.
- Triggered only on `at-risk`/`action-required` escalation transitions (not on `first`).
- `OWNER_NOTIFY_DISCORD_URL` env var is optional — fail-quiet if absent.
- **No code gap. This is intentional optional configuration.**

---

## Surfaced from Conversations

No transcript files found in `Docs/Transcripts/`. No prompt-miner output available (cortex blocked by hook 005). Skipped.

---

## Predicted Actions

### From Q1 Items (Do Now — code-executable, no owner needed)

1. `manual code fix in app/api/adopt-deferred-gifts/route.ts` — wire `handleGiftWelcomeForDate(today)` from `lib/payment-handlers.ts`; iterate Mollie/Stripe active subs with `gift_send_date===today` [2-3h]
2. `manual code fix in app/api/checkout/route.ts` — thread `donor_name` and `alpaca_name` into `success_url` template (extract from Stripe session metadata at checkout creation, or from metadata in `checkout.session.completed` webhook) [1h]
3. `manual code fix in lib/tenants/alpacasibiza.ts:31` — `cif: null` → `cif: 'ESY6917111J'` [5m] ⚡
4. `manual code fix in lib/tenants/alpacasibiza.ts` address block — `streetAddress: 'C/3 Bungalow Park 22'` (confirm postal code 07819 vs 07850 with owner first) [10m] ⚡
5. `manual code fix for Overlord item 1` — create `tests/e2e/adopt.spec.ts` + `tests/e2e/booking.spec.ts` per spec in `OVERLORD_QUEUE.md` [2-3h]

### From Q2 Items (Schedule)

1. `manual code fix in app/api/stripe-webhook/route.ts` — wire `upsertSubscriptionFromStripe` on `invoice.paid` renewal (DB layer is built; just missing the call) [30m] ⚡
2. `OVERLORD_QUEUE.md item 2` — bundle size analysis: enable `ANALYZE=true` in `next.config.*`, scan routes > 200 kB [1h]
3. `OVERLORD_QUEUE.md item 4` — sitemap rebuild: cross-reference noindex routes, remove excluded paths [30m] ⚡
4. `manual fix` — wire `lib/retry.ts:withRetry` into FareHarbor availability fetch (or delete if unused post-API-key-provisioning) [15m] ⚡

### Maintenance Due

- `/task-radar` — this report is current ✓
- No `command-history.jsonl` found; skipping maintenance staleness check

---

### Quick Wins (parallel-safe, no owner input, ≤30m each)

⚡ **CIF 1-liner** — `lib/tenants/alpacasibiza.ts:31`: `cif: null` → `cif: 'ESY6917111J'` [5m]
⚡ **Address streetAddress fix** — same file: `streetAddress: 'C/3 Bungalow Park 22'` (note postal code conflict — confirm 07850 before applying `postalCode`) [10m]
⚡ **DB renewal upsert** — `app/api/stripe-webhook/route.ts case 'invoice.paid'`: add `await upsertSubscriptionFromStripe(...)` call after `handleStripeInvoicePaid(...)` succeeds [30m]
⚡ **Sitemap noindex purge** — grep `noindex` from `app/` metadata exports, remove from `app/sitemap.ts` [30m]
⚡ **Delete or wire format-price + retry** — `lib/format-price.ts` + `lib/retry.ts` — remove if permanently unused, otherwise wire into tour card / FH fetch [15m]

---

## L3 Pipeline Audit — Completion Estimates

| Item | Evidence Level | Completion % |
|------|---------------|-------------|
| Adopt-deferred-gifts cron | Stub only — no handler invoked | 25% |
| success_url donor_name threading | Component exists, checkout route missing threading | 40% |
| CIF / address integration | Data in FORWARD_PLAN, 1-line edit pending | 95% |
| cancel-feedback route | Route + form + page wired | 100% |
| adopters-wall / latest.ts | Full implementation with Stripe + Mollie paths | 100% |
| dunning recovery (handleStripeInvoicePaid) | Handler exists, wired in switch | 100% |
| social-proof.ts (static pool) | Intentionally empty, owner-blocked | 10% (owner-gated) |
| Discord notify | Env-var only, fully wired | 100% |
| E2E tests (Overlord item 1) | `tests/e2e/` only has 3 smoke specs + funnel spec; adopt.spec not per OVERLORD spec | 20% |
| DB renewal upsert | DB layer built, call missing from `invoice.paid` handler | 70% |

---

## Pending Specs Inventory

No `specs/todo/` directory exists. Spec count: 0 pending.

Completed specs: 4 (in `specs/done/`):
- `001-tour-price-single-source.md`
- `004-dead-routes-cleanup.md`
- `006-structured-data-integrity.md`
- `007-form-handler-dedup.md`

_(Note: START_HERE.md reports 7 done specs; only 4 found in specs/done/ via glob. Discrepancy — specs/done/ may be stale or START_HERE.md counts ADRs.)_

---

## Session Health

Brain CLI not available. Skipped.

---

## Trend Tracking

Fewer than 3 reports exist (this is rd-004, first in this radar series for the project — no rd-001/002/003 exist locally). Trend tracking unavailable.

> Trend tracking available after 3+ reports.

---

## Notes

- **No cortex writes performed.** Hook 005 blocks all `cortex_remember`/`cortex_update_memory` calls.
- **No previous radar file found** — this is the first `reports/task-radar/` report. Item ledger and resolution registry not yet created.
- **FORWARD_PLAN.md was updated 2026-05-31** (post-scrape gap recheck) — some items that appeared as gaps are now "integration pending" (CIF, address, alpaca bios, phone, yoga data, weaving studio copy). These are correctly classified as Q1 or Q2 depending on whether developer action is needed vs. owner sign-off.
- **Overlord cycles 1-4 shipped** (commits `081ce76`, `e1897b7`, `492c719`). Cycles 5+ (E2E, bundle, GDPR export, sitemap) are queued in `OVERLORD_QUEUE.md` but unexecuted — all appear as Q1/Q2 items above.

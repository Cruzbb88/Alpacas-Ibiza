# ROADMAP — Alpacas Ibiza redesign (2026-06-09)

**Built off:** `FORWARD_PLAN.md` (authoritative state) + live recon of the working tree.
**Purpose:** coordinate TWO AIs working this repo concurrently + a human owner. Defines who owns what so we don't collide or duplicate. Source of truth for "what's left."

---

## 0. The spec / milestone state (from FORWARD_PLAN.md)

- **Launch-blocking = ALL owner-action** (legal text, GDPR consent copy, payment keys, Stripe price IDs, Tier-1 secrets, Resend domain auth, Vercel/DNS cutover). No code unblocks these.
- **Open specs** in `specs/todo/`: 002 legal/GDPR · 003 adopt (largely shipped) · 005 locale strategy · 006 launch-readiness · 007 stripe keep-warm · 008 real-alpaca-content · 009 mollie-connect.
- The engineering scaffold is **ahead of its content** — the gap to "done" is owner content, not code.

---

## 1. LANE A — the OTHER AI (in flight; DO NOT TOUCH these files)

A wave of 6+ env-gated features, fully wired but uncommitted. Pattern: render null / `notFound()` until owner sets the env var.

| Feature | Status | Owner-env to go live |
|---|---|---|
| `/membership` + `/api/membership-checkout` + thank-you | DONE | `MEMBERSHIP_LIVE`, price ID |
| Junior tier (`JuniorTierCard` + `/api/junior-checkout`) | DONE | `JUNIOR_TIER_LIVE`, price ID |
| `/herd-family` landing | DONE | `HERD_FAMILY_LIVE` |
| `/redeem-voucher` + `/api/voucher-redeem` | DONE (scaffold) | `VALID_VOUCHER_CODES` |
| `SeasonalPriceList` + `TOUR_SEASONAL_WINDOWS` | DONE | windows JSON |
| `/api/translate` + `ReviewTranslateButton` | DONE | — (free provider) |
| `/tour-confirmation`, admin monitoring/setup, waitlist, newsletter archive, preferences | DONE | — |
| **Greeting-card payment metadata** (`giftCardDesign` → Stripe/Mollie) | **IN PROGRESS** (explicit TODO) | — |
| **`reviews.*` i18n keys** for ReviewTranslateButton | **LIKELY IN PROGRESS** | — |

**🚫 COLLISION ZONES (their lane — I will not edit):** the feature files above, plus shared infra they're actively extending: `lib/config.ts`, `components/header.tsx`, `components/mobile-sticky-booking-bar.tsx`, `lib/payment-handlers.ts`, `lib/payment-vendor.ts`, `lib/data/greeting-cards.ts`, `app/[locale]/adopt/page.tsx`, `components/adopt/adopt-gift-adoption.tsx`, `lib/structured-data.ts`, `CLAUDE.md`, and **`translations/en.json`** (receiving new `reviews.*`/nav keys — high churn). Security files: `lib/db/scrub-payload.ts`, `lib/integrations/captcha-recaptcha.ts`.

---

## 2. LANE B — MY lane (buildable now, complementary, no owner data)

Ordered by value × safety (non-collision first). ✅ = done this turn.

### B1 — Non-collision (isolated files)
1. ✅ **Tours OG image price** `€30 → €21.19` — `app/[locale]/tours/opengraph-image.tsx:57`. DONE.
2. ✅ **`loading.tsx` for 10 routes** (about, gifts, visit, yoga, weaving, sustainability, workshops, weddings, press, skein) — DONE, all 10 routes verified 200.
3. ⛔ **Alpaca profile enrichment** — MOVED TO LANE D (owner-blocked). The `Alpaca` interface has NO family-relationship or fun-facts fields and every `bio` is `null`. Building family hyperlinks / fun-facts carousel would require inventing owner data (Rule 5 violation). Needs owner to supply a relationship graph + fun facts before any UI can be wired.
4. ✅ **Journal newsletter cross-sell** — `NewsletterForm` added to journal index + post pages (`source=journal` / `journal-post`). DONE, verified rendering.
5. ✅ **Inline-schema postcode** `07819 → 07850` (canonical tenant value, live-site sourced) in `visit/page.tsx` + `yoga/page.tsx`. DONE.

### B2 — Touches `en.json` (collision-managed via mtime guard)
6. ✅ **Mojibake repair** — ALL 6 locales (de 275 / es 351 / fr 621 / it 86 / nl 28 / en 18 runs ≈ 1,380), 9 emoji restored. Byte-accurate cp1252+latin-1 in-place repair (preserves formatting/key-order). JSON valid; all 6 locale homepages verified mojibake-free at runtime. DONE.
7. ✅ **Remove fabricated `guestStories.*`** keys — classified first (Chesterton's Fence): `testimonial`/`guest`/`visited` confirmed DEAD (zero callers) + fabricated (generic unattributed testimonial). Removed from all 6 locales; real `reviews.{sue,verena,gemma}` (used on home + tours) retained. Pages still 200. DONE.
8. ◑ **Experiences fabrication cleanup** — YOGA DONE (verified against live `/alpaca-yoga` 2026-06-10): duration "1.5 hours"→"1 hour 15 minutes" (live = "1u 15 minuten") and removed fabricated "ages 15+" (no age on live) in BOTH en.json (subtitle/whatBody/details/desc) AND `yoga/page.tsx` schema description. Kept verified-correct max-6 + Wed/Sat + €30. STILL PENDING (need live verification before touching — don't mass-remove): corporate "Sample Itinerary" (live page `/business-incentives-brainstormsessies`), family "under 3 free / ages 4+" age policy. Refund FAQ left intact (plausibly real; likely in live "algemene voorwaarden").
9. ⏳ **Fill empty adopt keys** — PENDING.
10. ✅/⛔ **`gifts.faq`** — MOOT: no `gifts.faq` key exists in en.json (absent). Nothing to render; will NOT invent an FAQ. Closed.

### Bonus runtime bugs fixed this pass (not in original plan)
- ✅ **`nav.yoga` INSUFFICIENT_PATH crash** — `nav.yoga` was an object `{title:…}` (lone object among flat-string siblings); `footer.tsx:179` read it as a string → error on every page render (en + nl). Flattened to `"Alpaca Yoga"` string in en + nl. de/es/fr/it had it absent (footer falls back to English; their `yoga.title` is UNMAPPED-null so no real translation to use — left to graceful fallback). Verified: 0 errors post-fix, no restart needed.

### B3 — Review-card fake stars
11. ✅ **Removed hardcoded ⭐⭐⭐⭐⭐** in `components/review-card.tsx` — FB recommendations are binary (`rating:null`); 5 stars fabricated a score. Review text/name/date/FB-badge (all real data) retained. DONE.

### Already completed earlier this session (do NOT redo)
- Palette → sandy-beige + nature-green tokens (light+dark+sidebar)
- `/shop/woven` de-fabricated → UNMAPPED placeholders; 6 fake product keys removed from all 6 locales
- `/weaving/collection` merged into `/shop/woven` (redirect)
- Search `/commission` 404 fixed; junior cancel param; embedded return_url ADR-017
- Smooth-scroll, visible breadcrumbs, 5 loading skeletons
- ~63 hardcoded brand hexes → tokens (9 public files)

---

## 3. LANE C — DESIGN DECISIONS (need Cruz's UX call, not actioned)
- Inline FareHarbor on `/visit` (duplicates `/tours`) · Tiered adopt packages (Bronze/Silver/Gold) · Instagram feed embed placement · Journal newsletter CTA placement · About-page CTA placement.

## 4. LANE D — OWNER-BLOCKED (no AI can do)
Real photography (the #1 gap) · prices · FareHarbor item IDs · payment/Google/GA4 keys · legal text + GDPR copy · CIF/registered address · press assets · awards · discount codes · alpaca bios sign-off + 11 birth dates · env flags to flip features live.

---

## 5. Coordination protocol
- I edit **Lane B only**, never Lane A files. If a Lane-B item must touch a shared file (`en.json`), I read-immediately-before-edit and abort on "modified since read."
- Commits: none (owner pushes). Both AIs leave work uncommitted in the tree.
- This file is MY coordination artifact; the other AI's is `CLAUDE.md` failsafe map + `FORWARD_PLAN.md`.
